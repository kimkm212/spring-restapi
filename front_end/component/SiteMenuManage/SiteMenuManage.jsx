import React, { Component } from 'react';
import PropTypes from 'prop-types';

const propTypes = {};

const defaultProps = {};

class SiteMenuManage extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div />
    );
  }
}

SiteMenuManage.propTypes = propTypes;
SiteMenuManage.defaultProps = defaultProps;

export default SiteMenuManage;
